URL Shortner

## Install
`npm install` to install the app to the developer directory.

## Run
`npm start` to run the dev server.

## MongoDB Connection String
Initialize a Database and add the username, password and the database name in the database.js file in the root of the project.

## PWA
Install it as a Progressive Web App